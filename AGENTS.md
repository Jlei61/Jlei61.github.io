# 博客发布协议

本仓库的文章目标是把作者提供的初稿整理成可以直接发布的成熟双语博客。除非用户明确要求专题页，所有文章都必须使用统一、克制的普通博客版式，不创建独立 Hero、动画、渐变背景或文章专用视觉系统。

## 1. 内容整理

- 保留作者的科学主张、证据、限制和引用边界，不凭空增加结果、文献或机制解释。
- 先明确文章的一句话主线，再按“问题 → 方法地图 → 核心证据 → 解释 → 展望”整理段落。
- 标题使用简单、描述性的论文式标题。不要默认使用营销式数字标题、口号或 `FIELD NOTE` 标签。
- 同时维护 `.en.md` 与 `.zh.md`，使用相同的 `translationKey`、slug、章节结构、图片位置和引用集合。
- 英文版本按英文科研写作逻辑重写，不逐句直译；中文版本保持自然、准确、可读。
- 每篇长文显式设置 `estimatedReadingTime`，中文不能依赖 Hugo 的自动词数估算。

## 2. 统一页面格式

- 使用 PaperMod 的标准 single-post 模板和 `assets/css/extended/clean-blog.css`。
- 标准结构为：普通标题 → 日期 / 预计阅读时间 / 作者 → 折叠目录 → 正文。
- 正文桌面宽度约 720 px，系统无衬线字体，正文 16 px，行距约 1.7；移动端必须无横向溢出。
- 图片作为正文证据出现，不作为装饰性 Hero。每张图片必须有准确的 alt text 和中英文图注。
- 不为单篇文章创建专用 layout 或 CSS，除非用户明确要求并确认设计方向。

## 3. 数学公式硬性规则

- 行内公式只允许使用 `{{< mi >}}...{{< /mi >}}`。
- 展示公式只允许使用 `{{< math >}}...{{< /math >}}`。
- 禁止在正文中直接使用裸 `\(...\)`、`\[...\]`、`$$...$$` 或用普通方括号冒充公式边界；Hugo Markdown 可能转义、拆段或误解析其中的 `_`、`=`、`<`。
- shortcode 必须对内部 TeX 做 HTML escaping，避免公式中的 `<` 被浏览器当成标签。
- 公式运行时必须来自仓库内的 `/vendor/mathjax/tex-chtml.js` 和配套字体。禁止依赖 CDN；公式在断网或第三方资源被拦截时也必须显示。
- 更新 MathJax 时必须同步提交运行时、`output/chtml/fonts/woff-v2/` 和许可证文件。

## 4. 图片与静态资源

- 用户提供的图片复制到 `static/images/`，使用稳定、描述性的英文文件名。
- 复制后核对源文件与站内文件的 SHA-256；未经用户要求不要重绘、裁剪或改变科学图含义。
- 图片应在 390 px 手机宽度和桌面宽度下保持比例、清晰且不造成页面溢出。

## 5. 发布前验收门槛

每次发布必须完成以下检查，不能只看 Markdown 源码：

1. `python3 scripts/check_i18n.py` 通过。
2. 使用线上 CI 相同版本的 Hugo 完成 production build，且 `git diff --check` 通过。
3. 在真实浏览器中检查英文和中文页面、桌面和 390 px 手机宽度。
4. 浏览器中 `mjx-merror` 必须为 0。
5. `mjx-container` 总数必须等于文章中 `mi` 与 `math` shortcode 的总数；展示公式数必须等于 `math` shortcode 数。
6. 页面正文不得残留裸 `\(`、`\[`、`$$` 或 shortcode 文本。
7. 所有 MathJax 脚本和字体请求必须来自本站，页面不得请求 MathJax CDN。
8. 图片必须成功加载，`naturalWidth > 0`，页面无横向溢出。
9. 推送后等待 GitHub Pages workflow 成功，再检查中英文公开 URL 返回 200，并在公开页面重复公式计数与错误检查。

## 6. Git 边界

- 提交前先确认当前分支、远端同步状态和 dirty worktree。
- 只暂存本次博客工作涉及的文件；不得把已有的 Cortrix 或其他用户改动混入提交。
- 构建成功不等于线上成功。只有 GitHub Pages 部署完成且公开页面验收通过，才可报告发布完成。
